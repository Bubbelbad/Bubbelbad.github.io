/* This is not yet implemented, but it will be used to add interactivity when the mouse enters the buttons/texts */

const grid = document.getElementById('grid-portfolio');

grid.addEventListener('mouseenter', 
    function(event) {
        section1.style.backgroundColor = 'red';
    }
);

const galleryText = document.getElementById('galleryText');

something.addEventListener('mouseEnter',
    function(event) {
        something.style.backgroundColor = 'blue';
    }
);

const something = document.getElementById('galleryText');

something.addEventListener('mouseEnter',
    function(event) {
        something.style.backgroundColor = 'blue';
    }
);